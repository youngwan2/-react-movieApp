import React from 'react';
import styles from './Spinner.module.css'

const Spinner = () => {
    return (
        <article className={styles.loading_spinner}>
        </article>
    );
};

export default Spinner;